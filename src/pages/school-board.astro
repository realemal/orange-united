---
import BaseLayout from '../layouts/BaseLayout.astro';
import boardMembers from '../data/board-members.json';
---

<BaseLayout
  title="School Board Members"
  description="Meet the Orange Unified School District Board of Education. Learn about each trustee, their positions, and how to contact them."
>
  <div class="bg-gradient-to-br from-red-50 via-orange-50 to-white py-12">
    <div class="container-custom">
      <div class="text-center mb-8">
        <span class="inline-block px-4 py-1.5 bg-red-100 text-red-700 rounded-full text-sm font-semibold mb-4">Know Your Representatives</span>
        <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4" data-en="Orange Unified School Board" data-es="Junta Escolar Unificada de Orange">
          Orange Unified School Board
        </h1>
        <p class="text-xl text-gray-600 max-w-3xl mx-auto" data-en="These seven elected officials make critical decisions about curriculum, spending, and policies affecting Orange students." data-es="Estos siete funcionarios electos toman decisiones críticas sobre el currículo, gastos y políticas que afectan a los estudiantes de Orange.">
          These seven elected officials make critical decisions about curriculum, spending, and policies affecting Orange students.
        </p>
      </div>

    </div>
  </div>

  <!-- Board Members Section -->
  <section class="py-12">
    <div class="container-custom max-w-6xl">
      <div class="space-y-8">
        {boardMembers.map((member) => (
          <div class="card p-8 hover:shadow-2xl transition-all">
            <div class="grid md:grid-cols-4 gap-8">
              <!-- Photo -->
              <div class="md:col-span-1">
                <div class="relative overflow-hidden rounded-2xl bg-gradient-to-br from-gray-100 to-gray-50">
                  <img 
                    src={member.image}
                    alt={member.name}
                    class="w-full aspect-[3/4] object-cover"
                    onerror="this.src='/placeholder.png'"
                  />
                  <div class="absolute top-3 left-3">
                    <span class="bg-orange-500 text-white text-xs font-bold px-3 py-1.5 rounded-full shadow-lg">
                      Area {member.area}
                    </span>
                  </div>
                  {member.title !== 'Member' && (
                    <div class="absolute bottom-3 left-3 right-3">
                      <span class="bg-gray-900/90 text-white text-xs font-semibold px-3 py-1.5 rounded-full block text-center">
                        {member.title}
                      </span>
                    </div>
                  )}
                </div>
              </div>

              <!-- Info -->
              <div class="md:col-span-3 flex flex-col justify-center">
                <div>
                  <h2 class="text-3xl font-bold text-gray-900 mb-4">
                    {member.name}
                  </h2>
                  {member.title !== 'Member' && (
                    <p class="text-lg font-semibold text-gray-700 mb-3">
                      {member.title}
                    </p>
                  )}
                  <div class="space-y-2 mb-6">
                    <p class="text-lg text-orange-600 font-semibold">
                      Trustee Area {member.area}
                    </p>
                    <p class="text-base text-gray-600">
                      Current Term: {member.term}
                    </p>
                  </div>
                  
                  <!-- Contact -->
                  <div class="flex flex-wrap gap-3">
                    <a 
                      href={`mailto:${member.email}`}
                      class="btn btn-primary text-sm inline-flex items-center gap-2"
                    >
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                      </svg>
                      Email
                    </a>
                  </div>
                </div>
              </div>
            </div>
          </div>
        ))}
      </div>

      <!-- Call to Action -->
      <div class="mt-12 bg-gradient-to-br from-orange-500 to-orange-600 rounded-3xl p-8 text-white text-center shadow-2xl">
        <h2 class="text-3xl font-bold mb-4">Engage in Your Children's Education</h2>
        <p class="text-lg text-orange-100 mb-6 max-w-2xl mx-auto">
          Attend board meetings, stay informed on education policy decisions, and learn how to participate in the public process.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center mb-6">
          <a href="/events" class="btn bg-white text-orange-600 hover:bg-orange-50">
            View Meeting Schedule
          </a>
          <a href="/articles?category=Board%20Meetings" class="btn border-2 border-white text-white hover:bg-orange-700">
            Read Meeting Coverage
          </a>
        </div>
        <p class="text-orange-100 text-sm">
          Questions? <a href="mailto:info@orangeunited.org" class="text-white underline hover:text-orange-100">Email us at info@orangeunited.org</a>
        </p>
      </div>
    </div>
  </section>
</BaseLayout>

